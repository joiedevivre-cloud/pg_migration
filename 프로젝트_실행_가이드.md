# 마이그레이션 프로젝트 실행 가이드

## 📁 프로젝트 열기

1. **Cursor에서 열기**
   - Cursor를 실행하고 `File > Open Folder` 선택
   - `C:\Users\goeun` 폴더 선택

2. **또는 명령줄에서 열기**
   ```powershell
   cd C:\Users\goeun
   cursor .
   ```

## 🔧 환경 설정

### 1. 필요한 패키지 설치

프로젝트 루트 디렉토리에서 다음 명령 실행:

```powershell
pip install -r requirements.txt
```

설치되는 패키지:
- `cx_Oracle` / `oracledb`: Oracle 데이터베이스 연결
- `psycopg2-binary`: PostgreSQL 데이터베이스 연결
- `PyYAML`: YAML 설정 파일 파싱
- `pytest`: 테스트 실행

### 2. 설정 파일 확인

프로젝트에는 이미 `config.yaml` 파일이 있습니다. 필요시 수정:

**주요 설정 항목:**
- `oracle`: Oracle DB 접속 정보
- `postgres`: PostgreSQL DB 접속 정보
- `validation`: 검증 설정 (테이블 목록, 워커 수 등)

## 🚀 프로젝트 실행 방법

### 방법 1: 데이터 검증 실행 (권장)

```powershell
python run_validation.py
```

이 스크립트는:
- Oracle과 PostgreSQL에 연결
- `config.yaml`의 `validation.tables`에 지정된 테이블들을 검증
- 검증 결과를 JSON 리포트로 생성 (`reports/` 폴더)

### 방법 2: Python 코드에서 직접 사용

```python
from db.oracle import OracleDB
from db.postgres import PostgresDB
from validator.core.engine import ValidationEngine
from config.profile_loader import ProfileLoader
from checks.row_canonicalize import NullEmptyPolicy

# DB 연결
oracle_db = OracleDB("connection_string", "user", "password")
postgres_db = PostgresDB("host", 5432, "database", "user", "password")

# 검증 엔진 생성
engine = ValidationEngine(
    oracle_db=oracle_db,
    postgres_db=postgres_db,
    max_workers=5,
    max_concurrent_db_sessions=10,
    null_empty_policy=NullEmptyPolicy.DISTINCT,
    decimal_tolerance=0.0001
)

# 테이블 검증 실행
result = engine.validate_table(
    table_name="CF01",
    schema="IMSI",
    columns=None,  # None이면 자동 조회
    generate_report=True
)
```

### 방법 3: 테스트 실행

```powershell
# 모든 테스트 실행
pytest

# 특정 테스트 파일 실행
pytest tests/test_engine.py

# 상세 출력
pytest -v
```

## 📋 주요 파일 구조

```
C:\Users\goeun\
├── run_validation.py      # 검증 실행 메인 스크립트
├── config.yaml            # 데이터베이스 및 검증 설정
├── requirements.txt       # Python 패키지 의존성
├── README.md              # 프로젝트 설명서
├── db/                    # 데이터베이스 연결 모듈
│   ├── oracle.py
│   └── postgres.py
├── validator/             # 검증 엔진
│   └── core/
│       └── engine.py
├── config/                # 설정 로더
│   └── profile_loader.py
├── checks/                # 검증 체크 로직
├── profiles/              # 테이블별 검증 프로파일
│   └── tables.yaml.example
└── reports/               # 검증 결과 리포트 (자동 생성)
```

## ⚙️ 설정 파일 예시

### config.yaml 구조

```yaml
oracle:
  user: "imsi"
  password: "oracle123"
  dsn: "192.168.137.30:1521/adgdb"
  default_schema: "IMSI"

postgres:
  host: "192.168.137.101"
  port: 5432
  user: "postgres"
  password: "oracle123"
  dbname: "postgres"

validation:
  source_schema: "IMSI"
  target_schema: null  # null이면 source_schema와 동일
  tables:              # 빈 리스트면 모든 테이블
    - "CF01"
    - "CF02"
    - "CF03"
  max_workers: 5
  max_concurrent_db_sessions: 10
  decimal_tolerance: 0.0001
  null_empty_policy: "DISTINCT"
  profile_path: "profiles/tables.yaml"
```

## 📊 검증 결과 확인

검증이 완료되면 `reports/` 폴더에 JSON 리포트가 생성됩니다:
- 각 테이블별 검증 결과
- Chunk별 해시 비교 결과
- 집계 검증 결과 (COUNT, SUM 등)
- 불일치 발견 시 상세 정보

## 🔍 문제 해결

### 1. DB 연결 실패
- `config.yaml`의 접속 정보 확인
- 방화벽 설정 확인
- Oracle/PostgreSQL 서버 실행 상태 확인

### 2. 패키지 설치 오류
```powershell
# pip 업그레이드 후 재시도
python -m pip install --upgrade pip
pip install -r requirements.txt
```

### 3. 인코딩 오류
- Windows에서는 자동으로 UTF-8 인코딩 설정됨
- 문제 발생 시 `run_validation.py`의 인코딩 설정 확인

## 💡 팁

1. **프로파일 사용**: `profiles/tables.yaml`을 생성하여 테이블별 검증 모드 설정 가능
2. **병렬 처리**: `max_workers`와 `max_concurrent_db_sessions`로 성능 조절
3. **대용량 테이블**: 파티션 테이블은 자동으로 파티션 단위로 검증됨
4. **파티션 인식 검증**: 로그/시계열 테이블의 경우 `profiles/tables.yaml`에서 `validation_mode: partition_aware` 설정 가능

## 📊 파티션 인식 검증 설정

로그/시계열 테이블의 경우 파티션 인식 검증 모드를 사용할 수 있습니다:

### profiles/tables.yaml 예시

```yaml
IMSI:
  LOG_TABLE:
    columns:
      - log_id
      - log_date
      - log_level
      - message
    pk_columns:
      - log_id
    validation_mode:
      type: partition_aware
      partition_key: log_date
      immutable_before: "2024-01-01"  # 이 날짜 이전 파티션은 경량 검증
```

### 파티션 인식 검증 동작

- **오래된 파티션** (immutable_before 이전):
  - COUNT(*), SUM/MIN/MAX
  - Chunk Hash만 수행
  - Row-level drilldown 건너뜀 (리소스 절약)

- **최근 파티션**:
  - Chunk Hash
  - 제한적 Row-level drilldown (max_diffs_per_chunk)
  - 선택적 전체 Row 검증

### 리포트 확인

파티션 인식 검증을 사용하면 리포트에 `partition_summary` 섹션이 추가됩니다:
- `total_partitions`: 전체 파티션 수
- `fully_verified`: 전체 검증 완료 파티션 수
- `light_verified`: 경량 검증 완료 파티션 수
- `failed`: 검증 실패 파티션 수
